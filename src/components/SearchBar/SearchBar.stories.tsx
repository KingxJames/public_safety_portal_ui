// SearchBar.stories.tsx

import  { SearchBar } from './SearchBar'; // Make sure to use PascalCase for component names

export default {
  title: 'components/SearchBar', // PascalCase for storybook title
  component: SearchBar,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen',
  },
};

type Story = {
};

export const SearchBarComponent: Story = {
  
};