// Topbar.stories.tsx

import  { Topbar } from './Topbar'; // Make sure to use PascalCase for component names

export default {
  title: 'components/Topbar', // PascalCase for storybook title
  component: Topbar,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen',
  },
};

type Story = {
};

export const TopbarComponent: Story = {
  
};