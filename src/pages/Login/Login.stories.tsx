// Login.stories.tsx

import  { Login } from './Login'; // Make sure to use PascalCase for component names

export default {
  title: 'pages/Login', // PascalCase for storybook title
  component: Login,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen',
  },
};

type Story = {
};

export const LoginComponent: Story = {
  
};